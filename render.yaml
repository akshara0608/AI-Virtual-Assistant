services:
  # A web service
  - type: web
    name: ai-virtual-assistant
    env: python
    # Command to install dependencies
    buildCommand: "pip install -r requirements.txt"
    # Command to start your application
    startCommand: "gunicorn app:app" # Example for a Flask app in app.py
